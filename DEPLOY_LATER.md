# Deploy Later

Streamlit Community Cloud cannot call Ollama running on your personal computer. After local testing, deploy with either the built-in verified-data fallback or a hosted model configured through Streamlit Secrets. Never upload API keys to GitHub.
